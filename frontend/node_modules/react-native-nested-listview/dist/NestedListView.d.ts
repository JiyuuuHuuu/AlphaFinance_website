import React, { ReactElement } from 'react';
import { INode } from './NodeView';
export interface IProps {
    data: any;
    extraData?: any;
    renderNode: (item: INode, level: number, isLastLevel: boolean) => ReactElement;
    onNodePressed?: (item: INode) => void;
    getChildrenName?: (item: INode) => string;
    style?: StyleSheet;
    keepOpenedState?: boolean;
}
export interface IState {
    root: INode;
}
declare const NestedListView: React.MemoExoticComponent<({ getChildrenName, renderNode, data, onNodePressed, extraData, keepOpenedState, }: IProps) => JSX.Element>;
export { NestedListView };
